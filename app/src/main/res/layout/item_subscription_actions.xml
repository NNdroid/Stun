<?xml version="1.0" encoding="utf-8"?>
<!--
  订阅面板底部操作区（单滚动列表的最后一项）：添加订阅 / 从剪贴板粘贴 / 立即同步 + 同步进度。

  原先固定在面板底部，键盘弹出时和内嵌编辑框抢垂直空间；
  单滚动改造后跟随内容滚动，正在编辑的订阅能占据键盘上方的绝大部分区域。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_add_sub"
        style="@style/Widget.Material3.Button.TonalButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_2"
        android:text="@string/subscription_add"
        app:icon="@drawable/ic_add" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_paste"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_2"
        android:text="@string/paste_from_clipboard"
        app:icon="@drawable/ic_content_paste" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_sync"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_2"
        android:text="@string/subscription_sync_now"
        app:icon="@drawable/ic_sync" />

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/progress_sync"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_4"
        android:indeterminate="true"
        android:visibility="gone" />

</LinearLayout>
